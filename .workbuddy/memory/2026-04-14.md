# 2026-04-14 工作日志

## 多轮对话记忆功能实现（2026-04-15）
- **新建** ChatHistory.java: 对话历史实体（id/userId/role/content/createTime）
- **新建** ChatHistoryMapper.java: MyBatis-Plus Mapper，含查询历史+删除历史
- **新建** ChatHistoryService.java: 对话历史服务，loadHistoryForQwen()/saveConversation()/clearHistory()
- **新建** ChatHistoryMapper.xml: Mapper XML（deleteByUserId SQL）
- **修改** QwenService.java: 新增 chatWithHistory(history, userMsg, systemPrompt) 和 chatWithHistory(history, userMsg) 两个多轮方法
- **修改** AgentDispatcher.java: default分支+LIFE_SERVICE fallback均改为 chatWithHistory + saveConversation
- **新建** schema.sql: chat_history 建表SQL
- 逻辑：每次AI对话前加载最近10轮历史→拼装消息列表→调用→保存本轮对话

## 接入通义千问大模型到 campus-agent 校园助手项目
- **新建** `QwenService.java`: 通义千问API调用服务类，使用DashScope OpenAI兼容接口，支持带系统提示词的对话
- **修改** `AgentDispatcher.java`: 仅改 default 分支（UNKNOWN意图）从固定文案 fallback 到通义千问AI自由对话（带校园管家系统提示词）
- **修改** `application.yml`: 新增 qwen.api-key / qwen.model / qwen.url 配置段
- **修改** `pom.xml`: 新增 fastjson 1.2.83 依赖用于JSON解析
- **前端无需改动**: ChatBox.vue 仍走 /agent/chat 接口，返回格式完全兼容
- 用户需要自行替换 application.yml 中的 api-key 为真实值

## Bug修复：LIFE_SERVICE 空指针异常
- **问题**: LifeAgent.queryLifeService() 返回 null 时调用 .getAnswer() 导致空指针崩溃
- **修复**: AgentDispatcher.java 的 LIFE_SERVICE 分支加空判断，null 时 fallback 到通义千问 AI 对话

## Markdown格式适配（双管齐下）
- **后端**: AgentDispatcher.java 两处系统提示词（default分支 + LIFE_SERVICE fallback）新增"禁止使用Markdown格式"约束
- **前端**: ChatBox.vue 添加 renderMarkdown() 轻量渲染方法：
  - 纯文本 → 原样输出（零额外开销）
  - 含markdown符号 → 自动转为HTML渲染（标题/粗体/代码块/引用/列表等）
  - 用户消息不受影响，仅对AI消息生效
  - 使用 :deep() scoped样式美化渲染元素
