# 2026-04-17 工作记录

- 前端构建后复制到后端静态资源目录 `campus-agent/src/main/resources/static/`
- 添加 WebMvcConfig.java 支持 SPA 路由
- 添加 GET /agent/history 接口获取历史记录
- 修改 AgentDispatcher.java：在每次对话时自动保存用户消息和 AI 回复
- 修改 ChatBox.vue：mounted 时自动加载历史记录
- 用户需求：直接在 IDEA 里运行，不改端口（8080），启动时检查数据库记忆
- **修复问题**：
  - 去掉功能清单表格（恢复简洁界面）
  - 添加图片压缩功能（compressImage），限制图片宽度 800px，质量 0.7
  - 修复只有文档时调用 VL 模型报错的问题（改为走普通对话）
- **新增功能：文档解析模块**
  - 添加 pom.xml 依赖：Apache POI (Word/Excel)、Apache PDFBox (PDF)
  - 新增 DocumentParserService.java：解析 Word/Excel/PDF 文档
  - 修改 ChatController.java：接收 docBase64 和 docName 参数
  - 修改 ChatService.java：解析文档内容并拼接到消息
  - 修改 ImageChatController.java：支持文档参数
  - 修改 AgentDispatcher.java：支持文档 + 图片 + 文字混合输入
- **页面美化升级**
  - Navbar：渐变紫色背景、Logo图标、AI徽章、悬停动画
  - Home：浮动圆圈背景装饰、玻璃态卡片、底部提示标签
  - ChatBox：AI头像、欢迎语卡片、快捷问题标签、消息气泡动画
  - Remind.vue：渐变卡片、图标+内容列表、状态徽章、空状态引导
  - Schedule.vue：渐变卡片、课程时间线、彩色时间点、空状态提示
