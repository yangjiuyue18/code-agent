# 2026-04-20

## ChatBox.vue 界面重构
- 重新设计了聊天主界面布局，整体更自然协调
- 新增顶部标题栏（含头像、在线状态、导出/清空按钮）
- 欢迎区改为居中展示，快捷入口改为 4 列卡片网格（带悬停动效）
- 底部输入框区整合进统一输入条（圆角背景+聚焦高亮）
- 上传按钮改为紫色圆形 `+` 按钮，简洁有辨识度
- 欢迎区加 `v-if="msgList.length === 0"`，有消息后自动隐藏，不再随内容滚动
- 删除了 Home.vue 底部两个提示标签（💡 支持上传文件 / 🔒 隐私提示）
- 欢迎区改为独立渐变大卡片，和聊天区视觉分离
- 欢迎卡片支持收起/展开（右上角 `−` 收起，底部显示紫色展开条）
- 各子页面（Schedule/Repair/Remind/Dining）返回按钮统一改为紫色渐变胶囊 `← 返回`，悬停有左滑+放大效果
- 22:38 重新优化整个聊天界面布局：统一圆角风格（16px）、简化颜色方案、优化间距、输入框与欢迎卡片风格协调
